# @tellegen/svelte

Maintained by changesets: each entry is written from the changeset files that
shipped with the change (see [`.changeset/README.md`](../../.changeset/README.md)).

Releases through 0.1.2 predate this file and are recorded in the repository's
[root `CHANGELOG.md`](../../CHANGELOG.md), which is now the curated view across
the two npm packages and the `tellegen` crate.
