name: Deploy Demo

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions: {}

concurrency:
  group: tellegen-deploy-${{ github.ref_name }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

# ci.yml runs the gates on this same commit. The image job below builds and smoke
# tests the artifact that deploys, which is a stronger check than a repeat of a
# source-tree gate.
jobs:
  image:
    if: vars.TELLEGEN_DEPLOY_ENABLED == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      sha_tag: ${{ steps.meta.outputs.sha_tag }}
      main_tag: ${{ steps.meta.outputs.main_tag }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - name: Set image tags
        id: meta
        run: |
          image="ghcr.io/${GITHUB_REPOSITORY,,}"
          {
            echo "image=$image"
            echo "sha_tag=$image:${GITHUB_SHA}"
            echo "main_tag=$image:main"
          } >> "$GITHUB_OUTPUT"
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0
      - name: Build image (load locally for the smoke test)
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        with:
          context: .
          load: true
          tags: ${{ steps.meta.outputs.sha_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Smoke test image
        run: |
          docker rm -f tellegen-smoke >/dev/null 2>&1 || true
          docker run \
            --detach \
            --name tellegen-smoke \
            --publish 127.0.0.1:8000:8000 \
            --env TELLEGEN_ALLOW_FALLBACK=1 \
            --read-only \
            --tmpfs /tmp:size=64m,noexec,nosuid,nodev \
            --cap-drop ALL \
            --security-opt no-new-privileges \
            --pids-limit 512 \
            "${{ steps.meta.outputs.sha_tag }}"
          for attempt in $(seq 1 90); do
            if curl -fsS http://127.0.0.1:8000/api/health && curl -fsS http://127.0.0.1:8000/ | grep -q 'tellegen'; then
              exit 0
            fi
            echo "waiting for container health ($attempt/90)"
            sleep 10
          done
          docker logs --tail=200 tellegen-smoke
          exit 1
      - name: Stop smoke container
        if: always()
        run: docker rm -f tellegen-smoke >/dev/null 2>&1 || true
      - name: Log in to GHCR
        run: echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
      - name: Push image
        # Reuses the layers cached by the build above, so this is a cache hit that
        # only tags and pushes rather than rebuilding.
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        with:
          context: .
          push: true
          tags: |
            ${{ steps.meta.outputs.sha_tag }}
            ${{ steps.meta.outputs.main_tag }}
          cache-from: type=gha

  deploy:
    if: vars.TELLEGEN_DEPLOY_ENABLED == 'true'
    runs-on: ubuntu-latest
    needs: image
    environment: demo
    permissions:
      contents: read
    env:
      DEPLOY_HOST: ${{ secrets.TELLEGEN_DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.TELLEGEN_DEPLOY_USER }}
      DEPLOY_SSH_KEY: ${{ secrets.TELLEGEN_DEPLOY_SSH_KEY }}
      DEPLOY_PATH: ${{ secrets.TELLEGEN_DEPLOY_PATH }}
      DEMO_URL: ${{ secrets.TELLEGEN_DEMO_URL }}
      DEPLOY_KNOWN_HOSTS: ${{ secrets.TELLEGEN_DEPLOY_KNOWN_HOSTS }}
      TELLEGEN_IMAGE: ${{ needs.image.outputs.sha_tag }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false
      - name: Validate deploy secrets
        run: |
          for name in DEPLOY_HOST DEPLOY_USER DEPLOY_SSH_KEY DEPLOY_PATH DEMO_URL DEPLOY_KNOWN_HOSTS; do
            if [ -z "${!name}" ]; then
              echo "$name is not set"
              exit 1
            fi
          done
      - name: Configure SSH
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/tellegen_deploy
          chmod 600 ~/.ssh/tellegen_deploy
          printf '%s\n' "$DEPLOY_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
      - name: Copy deploy files
        run: |
          remote="$DEPLOY_USER@$DEPLOY_HOST"
          ssh -i ~/.ssh/tellegen_deploy "$remote" "mkdir -p '$DEPLOY_PATH/deploy'"
          scp -i ~/.ssh/tellegen_deploy \
            deploy/docker-compose.prod.yml \
            deploy/docker-compose.edge.yml \
            deploy/remote-deploy.sh \
            "$remote:$DEPLOY_PATH/deploy/"
      - name: Deploy image
        run: |
          remote="$DEPLOY_USER@$DEPLOY_HOST"
          ssh -i ~/.ssh/tellegen_deploy "$remote" "
            cd '$DEPLOY_PATH' &&
            bash deploy/remote-deploy.sh '$TELLEGEN_IMAGE' '$DEPLOY_PATH/data'
          "
      - name: Check public health
        run: |
          health_url="${DEMO_URL%/}/api/health"
          for attempt in $(seq 1 30); do
            health=$(curl -fsS "$health_url") || {
              echo "waiting for public health ($attempt/30)"
              sleep 10
              continue
            }
            # Gate on liveness (status ok with at least one case), not a hardcoded
            # case set, so adding/removing a case never breaks the deploy.
            if [[ "$health" == *'"status":"ok"'* && "$health" != *'"cases":[]'* ]]; then
              exit 0
            fi
            echo "unexpected public health payload: $health"
            if [ "$attempt" -lt 30 ]; then
              echo "waiting for public health ($attempt/30)"
              sleep 10
            fi
          done
          echo "public health check failed: $health_url"
          exit 1
