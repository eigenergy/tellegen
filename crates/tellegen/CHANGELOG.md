# Changelog

All notable changes to the `tellegen` crate. Maintained by release-plz
(`release-plz.toml`); entries below are written from the commits between tags.

Releases through 0.1.2 predate this file and are recorded in the repository's
[root `CHANGELOG.md`](../../CHANGELOG.md), which is now the curated view across
the crate and the two npm packages.
