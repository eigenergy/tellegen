# Replace with the deployment domain.
tellegen.example.org {
	encode zstd gzip
	reverse_proxy 127.0.0.1:8000

	# Add rate limits before broad public traffic. The expensive endpoints are
	# /api/cases/*/solve and /api/cases/*/sensitivity/*.
	# Keep /solve compatible with SSE: no response buffering, and write
	# timeouts long enough for the largest served case.
}
