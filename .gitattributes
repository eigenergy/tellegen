# The pglib MATPOWER fixtures are embedded (include_str!) as the offline fallback
# cases; they are upstream data, not project source. Mark them vendored so they
# don't skew the GitHub language breakdown.
crates/tellegen-server/fixtures/pglib/*.m linguist-vendored
